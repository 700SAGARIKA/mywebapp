apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ecs-app.fullname" . }}-test-connection"
  labels:
    {{- include "ecs-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ecs-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
