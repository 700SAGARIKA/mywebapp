apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "ecs-app.fullname" . }}
spec:
  minAvailable: 1
  selector:
    matchLabels:
      {{- include "ecs-app.selectorLabels" . | nindent 6 }}
